# Samba Web Management Panel

> [!IMPORTANT]
> **Внимание!** Данная панель является нодой и **не работает автономно**. Для её функционирования (аутентификации операторов) требуется предварительно развернутый сервер авторизации [Centralized Authentication Server](https://github.com/<YOUR_GITHUB_USERNAME>/centralized-auth).

Легковесная, современная и безопасная веб-панель на Python/Flask для управления локальными пользователями Samba и разграничения прав доступа к сетевым каталогам на основе системных групп Linux.

Интерфейс выполнен в премиальном темном стиле (Crimson Red / Glassmorphism) в тон панели управления VPN.

---

## Основные возможности

1. **Матрица прав доступа в реальном времени**:
   - Отображение прав всех активных сотрудников на сетевые каталоги в виде наглядной сетки.
   - Управление доступом в один клик: **RW** (Чтение и запись), **RO** (Только чтение), **—** (Доступа нет).
   - Изменения мгновенно применяются на уровне системных групп Linux (`gpasswd`).
   - Автоматическое скрытие заблокированных пользователей из рабочей матрицы.

2. **Управление пользователями Samba**:
   - Создание локальных пользователей Samba (автоматическая синхронизация с системными учетными записями `/etc/passwd` и базой `smbpasswd`).
   - Обязательное указание настоящего имени и фамилии сотрудника (ФИО) для удобства администрирования.
   - Редактирование учетной записи (смена логина и ФИО с сохранением хеша пароля без необходимости сброса).
   - Быстрый сброс пароля (с генератором надежных паролей).
   - Блокировка и разблокировка пользователей (временное отключение доступа к Samba).
   - Сортировка по алфавиту и живой поиск по логину или ФИО.

3. **Временные ссылки для клиентов и руководства**:
   - Генерация уникальных временных ссылок на защищенную от изменений (Read-Only) матрицу прав для демонстрации клиентам или руководству.
   - Настройка срока действия ссылки (1, 3, 7 или 30 дней) и добавление описания цели создания.
   - Список активных ссылок в панели администратора с возможностью мгновенного отзыва доступа.

4. **Экспорт матрицы в Excel (`.xlsx`)**:
   - Выгрузка всей текущей матрицы прав в красиво отформатированный файл Excel.
   - Автоматическая цветовая разметка ячеек: зеленый для **RW**, желтый для **RO**, серый для каталогов без ограничений.
   - Доступно как оператору в панели управления, так и клиентам при просмотре отчета по временной ссылке.

5. **Централизованная авторизация операторов**:
   - Авторизация в панели делегирована внешнему серверу аутентификации (`Central Auth Server`) по токену ноды (`node_api_token`).

6. **Журнал аудита**:
   - Логирование всех действий операторов (вход, создание/редактирование/блокировка пользователей, изменение прав доступа, создание/отзыв ссылок) в локальную базу данных ноды `samba-node.db`.

---

## Архитектура и интеграция с ОС

Веб-панель не вносит изменений в конфигурационный файл `smb.conf` и не создает каталоги на диске самостоятельно. Она считывает конфигурацию Samba (секции ресурсов, пути и привязанные группы доступа) и управляет членством пользователей в соответствующих системных группах:

Пример ресурса в `smb.conf`:
```ini
[Бухгалтерия]
        path = /share/Бухгалтерия
        valid users = @G-buh,@G-buh-r
        write list = @G-buh
        read list = @G-buh,@G-buh-r
        force group = G-buh
        create mask = 0660
        directory mask = 0771
```

Панель определит группы `@G-buh` (чтение-запись) и `@G-buh-r` (только чтение) и будет добавлять или удалять выбранного пользователя из этих системных групп Linux.

---

## Требования к окружению

- ОС Linux (Debian / Ubuntu)
- Установленный и настроенный сервер Samba
- Python 3.10+
- Системный пакет `python3-openpyxl` (для экспорта в Excel)
- Права `sudo` без ввода пароля (`NOPASSWD`) для пользователя, под которым запускается панель, на выполнение утилит:
  - `/usr/bin/pdbedit`
  - `/usr/sbin/useradd`, `/usr/sbin/usermod`
  - `/usr/bin/passwd`, `/usr/bin/smbpasswd`
  - `/usr/bin/gpasswd`

---

## Установка и развертывание

1. **Клонирование репозитория и создание сервисного аккаунта**:
   Склонируйте репозиторий в каталог `/opt/samba-web`:
   ```bash
   sudo git clone git@github.com:<YOUR_GITHUB_USERNAME>/samba-web.git /opt/samba-web
   ```
   Создайте выделенного системного пользователя `samba-web` без пароля и домашней папки:
   ```bash
   sudo useradd -r -s /bin/false -d /opt/samba-web samba-web
   ```
   Установите права владельца на каталог с проектом для созданного пользователя:
   ```bash
   sudo chown -R samba-web:samba-web /opt/samba-web
   ```

2. **Установка Python зависимостей**:
   Установите необходимые библиотеки (Flask, requests, openpyxl) с помощью `pip` (или через системный пакетный менеджер):
   ```bash
   pip3 install -r /opt/samba-web/requirements.txt
   ```

3. **Настройка прав `sudo`**:
   Для того чтобы панель, работающая под ограниченным сервисным пользователем `samba-web`, могла управлять системными учетными записями Samba, добавьте в `/etc/sudoers.d/samba-web` следующие права:
   ```text
   samba-web ALL=(ALL) NOPASSWD: /usr/bin/pdbedit, /usr/sbin/useradd, /usr/sbin/usermod, /usr/bin/passwd, /usr/bin/smbpasswd, /usr/bin/gpasswd
   ```

4. **Конфигурация приложения**:
   Отредактируйте файл `/opt/samba-web/config.json`:
   ```json
   {
     "mode": "node",
     "secret_key": "ВАШ_СЛУЧАЙНЫЙ_СЕКРЕТНЫЙ_КЛЮЧ",
     "central_auth_url": "http://<CENTRAL_AUTH_SERVER_IP>:5001",
     "node_api_token": "ТОКЕН_АВТОРИЗАЦИИ_НОДЫ_НА_СЕРВЕРЕ",
     "bind_host": "0.0.0.0",
     "bind_port": 5002,
     "smb_conf_path": "/etc/samba/smb.conf"
   }
   ```

5. **Настройка Systemd службы**:
   Скопируйте и запустите службу `samba-web.service` (она по умолчанию настроена на запуск от пользователя `User=samba-web`):
   ```bash
   sudo cp /opt/samba-web/samba-web.service /etc/systemd/system/
   sudo systemctl daemon-reload
   sudo systemctl enable samba-web
   sudo systemctl start samba-web
   ```

6. **Проверка логов**:
   ```bash
   sudo journalctl -u samba-web -f
   ```
